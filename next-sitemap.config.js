/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://s-worktint.com/', // Replace with your site's URL
  generateRobotsTxt: true, // (optional)
  sitemapSize: 50000, // Ensure this is large enough to cover all your URLs
}
